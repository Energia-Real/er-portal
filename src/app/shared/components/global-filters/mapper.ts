import { FormatsService } from '@app/shared/services/formats.service';
import * as entity from './global-filters-model';

export class Mapper {
	static getDataFilters(response: any, formatsService: FormatsService) {
		// console.log('MAPPER', response);
	}
}
